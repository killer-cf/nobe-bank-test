<section>
  <div>
    <%= form_with url: send_transfer_path, method: :patch, html: {class: 'flex flex-col w-[400px]'} do |form| %>
      <h1 class='text-2xl font-bold text-center'>Transferências</h1>
      <%= form.label :name, 'Nome' %>
      <%= form.text_field :name %>

      <%= form.label :cpf, 'CPF' %>
      <%= form.text_field :cpf %>

      <%= form.label :value, 'Valor' %>
      <%= form.number_field :value %>

      <%= form.submit "CONFIRMAR", class: 'btn-primary' %>
    <% end %>
  </div>
</section>